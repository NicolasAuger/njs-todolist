extends ../layout

block content
  h1= title
  br
  br
  form(
    method="post"
    action=action
    id="create_form"
  )
      h2 Modification de tâche
      | Modifier l'auteur
      input(name="author" placeholder=todo.author value=todo.author)
      | Modifier la tâche
      input(name="task" placeholder=todo.task value=todo.task)
      | Modifier le destinataire
      input(name="recipient" placeholder=todo.recipient value=todo.recipient)

      input(type="submit" id="create_form_submit" value="Modifier")
